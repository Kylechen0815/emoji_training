1.CelebA Dataset  它包含 20 萬張人臉，且每張臉都標註了 5 個關鍵點（雙眼、鼻子、嘴角），非常適合作為從 0 開始的訓練起步。

2.卷積 convolution（特徵提取）、池化 pooling（降維） 與 損失函數 lossing（誤差評估）

3.Epoch (輪次): 整個數據集完整訓練過一次的週期。

Learning Rate (學習率): 模型更新權重時的「步伐大小」。

Normalization (正規化): 將原始數值（如 0~128 像素）縮放到特定範圍（如 0~1），有助於運算穩定。